// this source generated by GenAI pythonProject
import API from "../../utils/api";

const SdAcDet = {

    insertRecord: (data) =>
        new Promise(function (resolve, reject) {
            API.post("/sdAcDet/insertRecord", data, {
                headers: {
                    "x-access-token": global.config.accessToken,
                },
            })
                .then(async (res) => {
                    if (res.status === 200) {
                        resolve(res.data);
                    } else {
                        reject(res.data.msg);
                    }
                })
                .catch((err) => {
                    reject(err);
                });
        }),

    updateRecord: (data) =>
        new Promise(function (resolve, reject) {
            API.patch("/sdAcDet/updateRecord", data, {
                headers: {
                    "x-access-token": global.config.accessToken,
                },
            })
                .then(async (res) => {
                    resolve(res.data);
                })
                .catch((err) => {
                    reject(err);
                });
        }),
    
	getRecord: (filter) =>
        new Promise(function (resolve, reject) {
            const baseUrl = "/sdAcDet/getRecord";
            const filterUrl = SdAcDet.getFilterUrl(filter);
            API.get(`${baseUrl}?${filterUrl}`, {
                headers: {
                    "x-access-token": global.config.accessToken,
                },
            })
                .then(async (res) => {
                    resolve(SdAcDet.formatRecord(res.data));
                })
                .catch((err) => {
                    reject(err);
                });
        }),

    formatRecord: (data) => {
        const formatted = [];

        for (let d of data) {
            formatted.push({
                

            id: d.id,
            ac_name: d.ac_name,
            descr: d.descr,
            us_id: d.us_id,
            us_name: d.us_name,
            imp_status_name: d.imp_status_name,
            ac_scenario: d.ac_scenario,
            ac_given: d.ac_given,
            ac_when: d.ac_when,
            ac_then: d.ac_then,
            testing_type_id: d.testing_type_id,
            proj_imp_status_id: d.proj_imp_status_id,
            clt_ref_id: d.clt_ref_id,
            lc_status_id: d.lc_status_id,
            is_active: d.is_active,
            created_at: d.created_at,
            created_by: d.created_by,
            tnnt_id: d.tnnt_id,

            });
        }
        return formatted;
    },

	getPageDet: (filter) =>
        new Promise(function (resolve, reject) {
            const baseUrl = "/sdAcDet/getPageDet";
            const filterUrl = SdAcDet.getFilterUrl(filter);
            API.get(`${baseUrl}?${filterUrl}`, {
                headers: {
                    "x-access-token": global.config.accessToken,
                },
            })
                .then(async (res) => {
                    resolve(SdAcDet.formatPageDet(res.data));
                })
                .catch((err) => {
                    reject(err);
                });
        }),

    formatPageDet: (data) => {
        const formatted = [];

        for (let d of data) {
            formatted.push({
                comment_cnt: d.comment_cnt == null ? 0 : d.comment_cnt,
				

            id: d.id,
            ac_name: d.ac_name,
            descr: d.descr,
            us_id: d.us_id,
            us_name: d.us_name,
            imp_status_name: d.imp_status_name,
            ac_scenario: d.ac_scenario,
            ac_given: d.ac_given,
            ac_when: d.ac_when,
            ac_then: d.ac_then,
            testing_type_id: d.testing_type_id,
            proj_imp_status_id: d.proj_imp_status_id,
            clt_ref_id: d.clt_ref_id,
            lc_status_id: d.lc_status_id,
            is_active: d.is_active,
            created_at: d.created_at,
            created_by: d.created_by,
            tnnt_id: d.tnnt_id,

            });
        }
        return formatted;
    },
    getAuditData: (filter) =>
        new Promise(function (resolve, reject) {
            const baseUrl = "/sdAcDet/getAudit";
            const filterUrl = SdAcDet.getFilterUrl(filter);
            API.get(`${baseUrl}?${filterUrl}`, {
                headers: {
                    "x-access-token": global.config.accessToken,
                },
            })
                .then(async (res) => {
                    console.log("res.data: ", res.data);
                    resolve(res.data);
                })
                .catch((err) => {
                    reject(err);
                });
        }),

    getFilteredData: (filter) =>
        new Promise(function (resolve, reject) {
            const baseUrl = "/sdAcDet/getFilteredData";
            const filterUrl = SdAcDet.getFilterUrl(filter);
            API.get(`${baseUrl}?${filterUrl}`, {
                headers: {
                    "x-access-token": global.config.accessToken,
                },
            })
                .then(async (res) => {
                    resolve(SdAcDet.formatFiltered(res.data));
                })
                .catch((err) => {
                    reject(err);
                });
        }),

    formatFiltered: (data) => {
        const formatted = [];

        for (let d of data) {
            formatted.push({
                

            id: d.id,
            ac_name: d.ac_name,
            descr: d.descr,
            us_id: d.us_id,
            us_name: d.us_name,
            imp_status_name: d.imp_status_name,
            ac_scenario: d.ac_scenario,
            ac_given: d.ac_given,
            ac_when: d.ac_when,
            ac_then: d.ac_then,
            testing_type_id: d.testing_type_id,
            proj_imp_status_id: d.proj_imp_status_id,
            clt_ref_id: d.clt_ref_id,
            lc_status_id: d.lc_status_id,
            is_active: d.is_active,
            created_at: d.created_at,
            created_by: d.created_by,
            tnnt_id: d.tnnt_id,

            });
        }
        return formatted;
    },

    getFilterUrl: (filter) => {
        let url = "";

        if (filter !== undefined) {
			if (filter.com_id !== undefined) {
              url += "com_id=" + filter.com_id + "&";
            }

if (filter.id !== undefined) {
    url += "id=" + filter.id + "&";
}
if (filter.ac_name !== undefined) {
    url += "ac_name=" + filter.ac_name + "&";
}
if (filter.descr !== undefined) {
    url += "descr=" + filter.descr + "&";
}
if (filter.us_id !== undefined) {
    url += "us_id=" + filter.us_id + "&";
}
if (filter.ac_scenario !== undefined) {
    url += "ac_scenario=" + filter.ac_scenario + "&";
}
if (filter.ac_given !== undefined) {
    url += "ac_given=" + filter.ac_given + "&";
}
if (filter.ac_when !== undefined) {
    url += "ac_when=" + filter.ac_when + "&";
}
if (filter.ac_then !== undefined) {
    url += "ac_then=" + filter.ac_then + "&";
}
if (filter.testing_type_id !== undefined) {
    url += "testing_type_id=" + filter.testing_type_id + "&";
}
if (filter.proj_imp_status_id !== undefined) {
    url += "proj_imp_status_id=" + filter.proj_imp_status_id + "&";
}
if (filter.clt_ref_id !== undefined) {
    url += "clt_ref_id=" + filter.clt_ref_id + "&";
}
if (filter.lc_status_id !== undefined) {
    url += "lc_status_id=" + filter.lc_status_id + "&";
}
if (filter.is_active !== undefined) {
    url += "is_active=" + filter.is_active + "&";
}
if (filter.created_at !== undefined) {
    url += "created_at=" + filter.created_at + "&";
}
if (filter.created_by !== undefined) {
    url += "created_by=" + filter.created_by + "&";
}
if (filter.tnnt_id !== undefined) {
    url += "tnnt_id=" + filter.tnnt_id + "&";
}
if (filter.ref_id !== undefined) { 
 url += "ref_id=" + filter.ref_id + "&";
}
			//param_value
            
        }
        return url;
    },
};

export default SdAcDet;