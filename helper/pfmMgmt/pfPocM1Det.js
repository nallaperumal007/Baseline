// this source generated by GenAI v2.9.4 
import API from "../../utils/api";

const PfPocM1Det = {

    insertRecord: (data) =>
        new Promise(function (resolve, reject) {
            API.post("/pfPocM1Det/insertRecord", data, {
                headers: {
                    "x-access-token": global.config.accessToken,
                },
            })
                .then(async (res) => {
                    if (res.status === 200) {
                        resolve(res.data);
                    } else {
                        reject(res.data.msg);
                    }
                })
                .catch((err) => {
                    reject(err);
                });
        }),

    updateRecord: (data) =>
        new Promise(function (resolve, reject) {
            API.patch("/pfPocM1Det/updateRecord", data, {
                headers: {
                    "x-access-token": global.config.accessToken,
                },
            })
                .then(async (res) => {
                    resolve(res.data);
                })
                .catch((err) => {
                    reject(err);
                });
        }),
    
	getRecord: (filter) =>
        new Promise(function (resolve, reject) {
            const baseUrl = "/pfPocM1Det/getRecord";
            const filterUrl = PfPocM1Det.getFilterUrl(filter);
            API.get(`${baseUrl}?${filterUrl}`, {
                headers: {
                    "x-access-token": global.config.accessToken,
                },
            })
                .then(async (res) => {
                    resolve(PfPocM1Det.formatRecord(res.data));
                })
                .catch((err) => {
                    reject(err);
                });
        }),

    formatRecord: (data) => {
        const formatted = [];

        for (let d of data) {
            formatted.push({
                

            id: d.id,
            base_com_id: d.base_com_id,
            base_com_rec_id: d.base_com_rec_id,
            poc_name: d.poc_name,
            design_id: d.design_id,
            mobile_nr: d.mobile_nr,
            alt_mobile_nr: d.alt_mobile_nr,
            email_id: d.email_id,
            alt_email_id: d.alt_email_id,
            wapp_nr: d.wapp_nr,
            lc_status_id: d.lc_status_id,
            is_active: d.is_active,
            created_at: d.created_at,
            created_by: d.created_by,
            tnnt_id: d.tnnt_id,

            });
        }
        return formatted;
    },

	getPageDet: (filter) =>
        new Promise(function (resolve, reject) {
            const baseUrl = "/pfPocM1Det/getPageDet";
            const filterUrl = PfPocM1Det.getFilterUrl(filter);
            API.get(`${baseUrl}?${filterUrl}`, {
                headers: {
                    "x-access-token": global.config.accessToken,
                },
            })
                .then(async (res) => {
                    resolve(PfPocM1Det.formatPageDet(res.data));
                })
                .catch((err) => {
                    reject(err);
                });
        }),

    formatPageDet: (data) => {
        const formatted = [];

        for (let d of data) {
            formatted.push({
                comment_cnt: d.comment_cnt == null ? 0 : d.comment_cnt,
				

            id: d.id,
            base_com_id: d.base_com_id,
            base_com_rec_id: d.base_com_rec_id,
            poc_name: d.poc_name,
            design_id: d.design_id,
            mobile_nr: d.mobile_nr,
            alt_mobile_nr: d.alt_mobile_nr,
            email_id: d.email_id,
            alt_email_id: d.alt_email_id,
            wapp_nr: d.wapp_nr,
            lc_status_id: d.lc_status_id,
            is_active: d.is_active,
            created_at: d.created_at,
            created_by: d.created_by,
            tnnt_id: d.tnnt_id,

            });
        }
        return formatted;
    },
    getAuditData: (filter) =>
        new Promise(function (resolve, reject) {
            const baseUrl = "/pfPocM1Det/getAudit";
            const filterUrl = PfPocM1Det.getFilterUrl(filter);
            API.get(`${baseUrl}?${filterUrl}`, {
                headers: {
                    "x-access-token": global.config.accessToken,
                },
            })
                .then(async (res) => {
                    console.log("res.data: ", res.data);
                    resolve(res.data);
                })
                .catch((err) => {
                    reject(err);
                });
        }),

    getFilteredData: (filter) =>
        new Promise(function (resolve, reject) {
            const baseUrl = "/pfPocM1Det/getFilteredData";
            const filterUrl = PfPocM1Det.getFilterUrl(filter);
            API.get(`${baseUrl}?${filterUrl}`, {
                headers: {
                    "x-access-token": global.config.accessToken,
                },
            })
                .then(async (res) => {
                    resolve(PfPocM1Det.formatFiltered(res.data));
                })
                .catch((err) => {
                    reject(err);
                });
        }),

    formatFiltered: (data) => {
        const formatted = [];

        for (let d of data) {
            formatted.push({
                

            id: d.id,
            base_com_id: d.base_com_id,
            base_com_rec_id: d.base_com_rec_id,
            poc_name: d.poc_name,
            design_id: d.design_id,
            mobile_nr: d.mobile_nr,
            alt_mobile_nr: d.alt_mobile_nr,
            email_id: d.email_id,
            alt_email_id: d.alt_email_id,
            wapp_nr: d.wapp_nr,
            lc_status_id: d.lc_status_id,
            is_active: d.is_active,
            created_at: d.created_at,
            created_by: d.created_by,
            tnnt_id: d.tnnt_id,

            });
        }
        return formatted;
    },

    getFilterUrl: (filter) => {
        let url = "";

        if (filter !== undefined) {
			if (filter.com_id !== undefined) {
              url += "com_id=" + filter.com_id + "&";
            }

if (filter.id !== undefined) {
    url += "id=" + filter.id + "&";
}
if (filter.base_com_id !== undefined) {
    url += "base_com_id=" + filter.base_com_id + "&";
}
if (filter.base_com_rec_id !== undefined) {
    url += "base_com_rec_id=" + filter.base_com_rec_id + "&";
}
if (filter.poc_name !== undefined) {
    url += "poc_name=" + filter.poc_name + "&";
}
if (filter.design_id !== undefined) {
    url += "design_id=" + filter.design_id + "&";
}
if (filter.mobile_nr !== undefined) {
    url += "mobile_nr=" + filter.mobile_nr + "&";
}
if (filter.alt_mobile_nr !== undefined) {
    url += "alt_mobile_nr=" + filter.alt_mobile_nr + "&";
}
if (filter.email_id !== undefined) {
    url += "email_id=" + filter.email_id + "&";
}
if (filter.alt_email_id !== undefined) {
    url += "alt_email_id=" + filter.alt_email_id + "&";
}
if (filter.wapp_nr !== undefined) {
    url += "wapp_nr=" + filter.wapp_nr + "&";
}
if (filter.lc_status_id !== undefined) {
    url += "lc_status_id=" + filter.lc_status_id + "&";
}
if (filter.is_active !== undefined) {
    url += "is_active=" + filter.is_active + "&";
}
if (filter.created_at !== undefined) {
    url += "created_at=" + filter.created_at + "&";
}
if (filter.created_by !== undefined) {
    url += "created_by=" + filter.created_by + "&";
}
if (filter.tnnt_id !== undefined) {
    url += "tnnt_id=" + filter.tnnt_id + "&";
}
if (filter.ref_id !== undefined) { 
 url += "ref_id=" + filter.ref_id + "&";
}
			//param_value
            
        }
        return url;
    },
};

export default PfPocM1Det;