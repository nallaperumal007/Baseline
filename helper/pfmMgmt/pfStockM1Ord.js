// this source generated by GenAI v2.9.4 
import API from "../../utils/api";
import moment from "moment";

const PfStockM1Ord = {

    insertRecord: (data) =>
        new Promise(function (resolve, reject) {
            API.post("/pfStockM1Ord/insertRecord", data, {
                headers: {
                    "x-access-token": global.config.accessToken,
                },
            })
                .then(async (res) => {
                    if (res.status === 200) {
                        resolve(res.data);
                    } else {
                        reject(res.data.msg);
                    }
                })
                .catch((err) => {
                    reject(err);
                });
        }),

    insertBatch: (data) =>
        new Promise(function (resolve, reject) {
            API.post("/pfStockM1Ord/insertBatch", data, {
                headers: {
                    "x-access-token": global.config.accessToken,
                },
            })
                .then(async (res) => {
                    if (res.status === 200) {
                        resolve(res.data);
                    } else {
                        reject(res.data.msg);
                    }
                })
                .catch((err) => {
                    reject(err);
                });
        }),

    updateRecord: (data) =>
        new Promise(function (resolve, reject) {
            API.patch("/pfStockM1Ord/updateRecord", data, {
                headers: {
                    "x-access-token": global.config.accessToken,
                },
            })
                .then(async (res) => {
                    resolve(res.data);
                })
                .catch((err) => {
                    reject(err);
                });
        }),

    updateBatch: (data) =>
        new Promise(function (resolve, reject) {
            API.patch("/pfStockM1Ord/updateBatch", data, {
                headers: {
                    "x-access-token": global.config.accessToken,
                },
            })
                .then(async (res) => {
                    resolve(res.data);
                })
                .catch((err) => {
                    reject(err);
                });
        }),

    getRecord: (filter) =>
        new Promise(function (resolve, reject) {
            const baseUrl = "/pfStockM1Ord/getRecord";
            const filterUrl = PfStockM1Ord.getFilterUrl(filter);
            API.get(`${baseUrl}?${filterUrl}`, {
                headers: {
                    "x-access-token": global.config.accessToken,
                },
            })
                .then(async (res) => {
                    resolve(PfStockM1Ord.formatRecord(res.data));
                })
                .catch((err) => {
                    reject(err);
                });
        }),

    getRecordForOrder: (filter) =>
        new Promise(function (resolve, reject) {
            const baseUrl = "/pfStockM1Ord/getRecordForOrder";
            const filterUrl = PfStockM1Ord.getFilterUrl(filter);
            API.get(`${baseUrl}?${filterUrl}`, {
                headers: {
                    "x-access-token": global.config.accessToken,
                },
            })
                .then(async (res) => {
                    resolve(PfStockM1Ord.formatRecord(res.data));
                })
                .catch((err) => {
                    reject(err);
                });
        }),

    formatRecord: (data) => {
        const formatted = [];

        for (let d of data) {
            formatted.push({


                id: d.id,
                invoice_nr: d.invoice_nr,
                raised_date: d.raised_date == null ? "" : d.raised_date,
                purchased_by: d.purchased_by,
                bill_amount: d.bill_amount,
                approved_by_id: d.approved_by_id,
                approved_date: d.approved_date == null ? "" : d.approved_date,
                business_justif: d.business_justif,
                fin_approval_date: d.fin_approval_date == null ? "" : d.fin_approval_date,
                settlement_amount: d.settlement_amount,
                ord_wfs_name:d.ord_wfs_name,
                ord_wfs_id: d.ord_wfs_id,
                lc_status_id: d.lc_status_id,
                is_active: d.is_active,
                created_at: d.created_at,
                created_by: d.created_by,
                tnnt_id: d.tnnt_id,

            });
        }
        return formatted;
    },

    getPageDet: (filter) =>
        new Promise(function (resolve, reject) {
            const baseUrl = "/pfStockM1Ord/getPageDet";
            const filterUrl = PfStockM1Ord.getFilterUrl(filter);
            API.get(`${baseUrl}?${filterUrl}`, {
                headers: {
                    "x-access-token": global.config.accessToken,
                },
            })
                .then(async (res) => {
                    resolve(PfStockM1Ord.formatPageDet(res.data));
                })
                .catch((err) => {
                    reject(err);
                });
        }),

    formatPageDet: (data) => {
        const formatted = [];

        for (let d of data) {
            formatted.push({
                comment_cnt: d.comment_cnt == null ? 0 : d.comment_cnt,
                clt_ref_id:d.clt_ref_id,
                ord_wfs_name:d.ord_wfs_name,
                id: d.id,
                invoice_nr: d.invoice_nr,
                raised_date: d.raised_date == null ? "" : d.raised_date,
                purchased_by: d.purchased_by,
                bill_amount: d.bill_amount,
                approved_by_id: d.approved_by_id,
                approved_date: d.approved_date == null ? "" : d.approved_date,
                business_justif: d.business_justif,
                fin_approval_date: d.fin_approval_date == null ? "" : d.fin_approval_date,
                settlement_amount: d.settlement_amount,
                ord_wfs_id: d.ord_wfs_id,
                lc_status_id: d.lc_status_id,
                is_active: d.is_active,
                created_at: d.created_at,
                created_by: d.created_by,
                tnnt_id: d.tnnt_id,

            });
        }
        return formatted;
    },
    getAuditData: (filter) =>
        new Promise(function (resolve, reject) {
            const baseUrl = "/pfStockM1Ord/getAudit";
            const filterUrl = PfStockM1Ord.getFilterUrl(filter);
            API.get(`${baseUrl}?${filterUrl}`, {
                headers: {
                    "x-access-token": global.config.accessToken,
                },
            })
                .then(async (res) => {
                    console.log("res.data: ", res.data);
                    resolve(res.data);
                })
                .catch((err) => {
                    reject(err);
                });
        }),

    getFilteredData: (filter) =>
        new Promise(function (resolve, reject) {
            const baseUrl = "/pfStockM1Ord/getFilteredData";
            const filterUrl = PfStockM1Ord.getFilterUrl(filter);
            API.get(`${baseUrl}?${filterUrl}`, {
                headers: {
                    "x-access-token": global.config.accessToken,
                },
            })
                .then(async (res) => {
                    resolve(PfStockM1Ord.formatFiltered(res.data));
                })
                .catch((err) => {
                    reject(err);
                });
        }),

    formatFiltered: (data) => {
        const formatted = [];

        for (let d of data) {
            formatted.push({


                id: d.id,
                invoice_nr: d.invoice_nr,
                raised_date: d.raised_date == null ? "" : d.raised_date,
                purchased_by: d.purchased_by,
                bill_amount: d.bill_amount,
                approved_by_id: d.approved_by_id,
                approved_date: d.approved_date == null ? "" : d.approved_date,
                business_justif: d.business_justif,
                fin_approval_date: d.fin_approval_date == null ? "" : d.fin_approval_date,
                settlement_amount: d.settlement_amount,
                ord_wfs_id: d.ord_wfs_id,
                lc_status_id: d.lc_status_id,
                is_active: d.is_active,
                created_at: d.created_at,
                created_by: d.created_by,
                tnnt_id: d.tnnt_id,

            });
        }
        return formatted;
    },

    getFilterUrl: (filter) => {
        let url = "";

        if (filter !== undefined) {
            if (filter.com_id !== undefined) {
                url += "com_id=" + filter.com_id + "&";
            }

            if (filter.id !== undefined) {
                url += "id=" + filter.id + "&";
            }
            if (filter.invoice_nr !== undefined) {
                url += "invoice_nr=" + filter.invoice_nr + "&";
            }
            if (filter.raised_date !== undefined) {
                url += "raised_date=" + filter.raised_date + "&";
            }
            if (filter.purchased_by !== undefined) {
                url += "purchased_by=" + filter.purchased_by + "&";
            }
            if (filter.bill_amount !== undefined) {
                url += "bill_amount=" + filter.bill_amount + "&";
            }
            if (filter.approved_by_id !== undefined) {
                url += "approved_by_id=" + filter.approved_by_id + "&";
            }
            if (filter.approved_date !== undefined) {
                url += "approved_date=" + filter.approved_date + "&";
            }
            if (filter.business_justif !== undefined) {
                url += "business_justif=" + filter.business_justif + "&";
            }
            if (filter.fin_approval_date !== undefined) {
                url += "fin_approval_date=" + filter.fin_approval_date + "&";
            }
            if (filter.settlement_amount !== undefined) {
                url += "settlement_amount=" + filter.settlement_amount + "&";
            }
            if (filter.ord_wfs_id !== undefined) {
                url += "ord_wfs_id=" + filter.ord_wfs_id + "&";
            }
            if (filter.lc_status_id !== undefined) {
                url += "lc_status_id=" + filter.lc_status_id + "&";
            }
            if (filter.is_active !== undefined) {
                url += "is_active=" + filter.is_active + "&";
            }
            if (filter.created_at !== undefined) {
                url += "created_at=" + filter.created_at + "&";
            }
            if (filter.created_by !== undefined) {
                url += "created_by=" + filter.created_by + "&";
            }
            if (filter.tnnt_id !== undefined) {
                url += "tnnt_id=" + filter.tnnt_id + "&";
            }
            if (filter.ref_id !== undefined) {
                url += "ref_id=" + filter.ref_id + "&";
            }
            //param_value

        }
        return url;
    },
};

export default PfStockM1Ord;